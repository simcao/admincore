{% extends 'admincore/base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-8">
                <div class="card">
                    <div class="card-title">
                        Mon formulaire
                    </div>
                    <div class="card-body">
                        {{ form(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}